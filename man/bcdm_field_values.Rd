% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/external.bcdm_field_values.R
\name{bcdm_field_values}
\alias{bcdm_field_values}
\title{Extract unique values from BCDM fields in the BOLD data package}
\usage{
bcdm_field_values(
  input.data,
  specific.cols,
  save.data = FALSE,
  output.file = NULL
)
}
\arguments{
\item{input.data}{Path to the input parquet file or the \code{bold_parquet_search} result.}

\item{specific.cols}{Name of the column to extract unique values from.}

\item{save.data}{Logical value indicating whether to save the results to disk as a .rds file (default: FALSE).}

\item{output.file}{Path (without extension) for saving results as .rds file (required if save.data = TRUE).}
}
\value{
A list containing unique values from the specified column. If \code{save.data} = T, a \code{.rds} file is exported locally.
}
\description{
Extracts distinct values of specified field(s) from the BOLD parquet data.
}
\details{
This function extracts unique values from one or more specified columns from the BOLD parquet data. It handles both the parquet file and \code{tbl_sql} objects from \code{bold_parquet_search} as input. The results can be saved to disk as an \code{.rds} file for later use.
}
\examples{
\donttest{

# Import the parquet file (This is a test parquet file composed of
# records of Cerambycidae beetles from Canada)
parquet_file <- system.file(
"extdata",
"test_data.parquet",
package = "BOLDNODE"
)

# Search the BOLD data package
bold_search <- bold_parquet_search(
  input.parquet = parquet_file,
  marker = "COI-5P"
)

# Get the field values#'
vocab.data <- bcdm_field_values(bold_search,
  specific.cols = c("inst", "identified_by")
)
}
}
